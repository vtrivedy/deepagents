# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Override default model
# OPENAI_MODEL=gpt-5-mini

# Modal configuration is handled via `modal setup` (stored in ~/.modal.toml)
